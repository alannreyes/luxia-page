# Optimización SEO + GEO (/citable) — 2026-07-28

Auditoría y corrección de la "capa de entendimiento" de luxia.us para que buscadores e IA
generativa entiendan **qué es, cómo se organiza y qué dice cada página** — y la citen.

## Capa 1 · Entidad (`app/layout.tsx`)
- Reescrito el JSON-LD `Organization` a la marca actual (antes tenía copy viejo "la IA a tu medida /
  consulta gratis" y `sameAs` errado `company/luxia`).
- Añadido `WebSite` JSON-LD (@graph con Organization).
- Enriquecido: `knowsAbout` (temas/entidades), `foundingDate: 2024`, `founder` (Alann Reyes),
  `areaServed`, `contactPoint`, y `sameAs` correcto → **LinkedIn `luxiaus` + GitHub `alannreyes`**
  (+ Wikidata Q-id pendiente de publicar).
- `og:image` ahora en todas las páginas (`lib/i18n/index.ts` → `generateLocalizedMetadata`).

## Capa 2 · Estructura
- Componente `components/Breadcrumbs.tsx` (migas visibles + `BreadcrumbList` JSON-LD).
- Cableado en `/services`, `/cases`, `/learning`.
- `BreadcrumbList` JSON-LD también en las plantillas `learning/[section]` y `cooking/[dish]`.

## Capa 3 · Contenido
- Componente `components/FAQ.tsx` (FAQ visible + `FAQPage` JSON-LD) en Home y Servicios (ES/EN).
- `Article` JSON-LD en las ~200 páginas de contenido (`learning/[section]`, `cooking/[dish]`):
  headline, description, `datePublished`, `dateModified: 2026-07-28`, author/publisher LuxIA,
  `articleSection` (nivel), image, url.

## Capa 4 · Índices IA + frescura (`app/robots.ts`)
- Crawlers de IA actualizados: GPTBot, OAI-SearchBot, ChatGPT-User, **ClaudeBot, Claude-User**,
  PerplexityBot, Perplexity-User, Google-Extended, Applebot-Extended (se quitaron los obsoletos
  Claude-Web / Anthropic-AI).
- (Ya existían: sitemap 213 URLs con `<lastmod>`, `/llms.txt`.)

## Medición
- **Umami self-host** en app04 (instancia compartida con Solvayu): host `stats.luxia.us` añadido al
  contenedor `umami-app`. Pendiente: DNS `stats.luxia.us → 86.48.21.142` + crear el website en el
  panel y pegar el website-id en el sitio.

## Pendientes
- Publicar entidad en **Wikidata** → agregar Q-id al `sameAs`.
- Token de **Google Search Console** (`verification.google` está vacío).
- (Opcional) FAQ por página en Learning/Cooking.

## Verificación
`node ~/.claude/skills/citable/scripts/audit.mjs https://luxia.us/es https://luxia.us/es/services https://luxia.us/es/learning/terminal`
→ Capas 1–4 ✓ (Home sin migas por ser raíz, es correcto).
